# HTTP server
PORT=5010

# Runtime provider: local | aws | azure
PROVIDER=local

# AWS / DynamoDB
AWS_REGION=us-east-1
USERS_TABLE=UsersTable

# Optional: local DynamoDB endpoint, for example http://localhost:8000
DYNAMO_ENDPOINT=
